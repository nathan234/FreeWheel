package org.freewheel.core.domain.dashboard

/**
 * Semantic color classification for metric progress thresholds.
 * Canonical source — [MetricType] uses a typealias to this.
 */
enum class ColorZone {
    GREEN, ORANGE, RED
}
